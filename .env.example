# No auto-load at startup (saves RAM). Accounts load on-demand when sending/checking.
AUTO_LOAD_ACCOUNTS=
# When auto-load is enabled, max accounts to load at startup (e.g. 5 or 10).
MAX_AUTO_LOAD_ACCOUNTS=
=
=
CHROME_PATH=
CHROME_HOME=
SESSION_PATH=
